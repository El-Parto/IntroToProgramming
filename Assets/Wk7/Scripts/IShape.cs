// no namespaces either...in this case anyway. Only for this case?

// Interfaces.
// always starts with captial I. No Objections No qualms Ienumerator is an interface.
// * Interfaces are blueprints for properties and functions.
// * The standard is to name them with a starting capital "I"
// * They cannot have any functionally within them as everything
// * is techncally abstract.
public interface IShape
{
    //Remember properties?
    float Height { get; }
    float Width { get; }
    string Name { get; }

}
